{
  "name": "studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Studio monorepo root — npm workspaces for the contract package, server, and (later) client",
  "author": "Tucker <connect@tucker.sh>",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run build --workspace @studio/contract && npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "test": "npm run build --workspace @studio/contract && npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace @studio/server",
    "start": "npm run start --workspace @studio/server"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "eslint": "^9.39.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "typescript-eslint": "^8.61.0"
  }
}
